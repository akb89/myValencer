function dispatch(data, rid) {
    switch (rid) {
    case 'frame':
        return null;
        // return <FrameComponent data={data} />;
    default:
        return null;
    }
}

export default dispatch;
